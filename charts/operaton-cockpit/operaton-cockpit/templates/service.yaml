apiVersion: v1
kind: Service
metadata:
  name: {{ include "operaton-cockpit.fullname" . }}
  labels:
    {{- include "operaton-cockpit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "operaton-cockpit.selectorLabels" . | nindent 4 }}
