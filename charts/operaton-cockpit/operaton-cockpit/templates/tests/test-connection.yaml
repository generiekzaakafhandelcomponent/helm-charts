---
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "operaton-cockpit.fullname" . }}-test-connection"
  labels:
    {{- include "operaton-cockpit.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "operaton-cockpit.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
